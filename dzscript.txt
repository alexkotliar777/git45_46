1.
Скопируйте URL-адрес репозитория на github
git clone "url" (вводим в терминале)
git remote -v


2.
Берем локальный каталог, который в настоящее время не находится под версионным 
контролем, и превратить его в репозиторий Git,
cd "полныйпуть к папке"
git clone «ссылка на гитхаб» 
git remote -v       
nano «создать новый скрипт»
cat «новый скрипт» 
git status  
git add <file> 
git commit -m «коммит к файлу»
git status 
git push

3.
git clone "сылка репазитория"

4.
git status   "проверяем статус нашего файла"
git remote -v "проверка на связку с github"

